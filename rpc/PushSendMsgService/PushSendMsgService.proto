syntax = "proto3";

package pushSendMsgService;
option go_package="./pushSendMsgService";

message Request {
  string ping = 1;
}

message Response {
  string pong = 1;
}

service PushSendMsgService {
  rpc Ping(Request) returns(Response);
}
